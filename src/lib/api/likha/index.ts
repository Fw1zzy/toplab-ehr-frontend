/**
 * Likha ERP adapter placeholder.
 * Replace the mock implementations below with real Likha ERP SDK calls
 * once NEXT_PUBLIC_LIKHA_URL and LIKHA_API_TOKEN are configured.
 *
 * Likha ERP follows the Directus API spec, so you can use the @directus/sdk
 * or any compatible client.
 */

export { mockAuth, mockPatients, mockEncounters, mockLabResults, mockInvoices, mockInventory, mockDashboard, mockAnalytics, mockServices } from '../mock';
